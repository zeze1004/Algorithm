#include <iostream>
#include <string.h>
#include <vector>
using namespace std;
#define N 100

int main() {
    ios_base :: sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    cout << "a";
    
    char *input, compare[N];
    for(char i = 'a'; i <= 'z'; i++) {
        compare[i] = i;
    }
    // 입력
    // cin >> input;
    


    return 0;
}
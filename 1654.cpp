#include <algorithm>
#include <iostream>
using namespace std;

int main()
{
	ios_base ::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int K, N, num[10001];
	cin >> K >> N;
	for (int i = 0; i < K; i++)
	{
		cin >> num[i];
	}
	while (1)
	{
	}

	return 0;
}